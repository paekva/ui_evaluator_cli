package com.tuc.masters.core.models

// Available in tests types of actions
enum class ActionType {
    FIND, FIND_ALL, CLICK, SEND_KEYS, SCROLL, SCRIPT, START, STOP, ASSERT, WAIT, LINK, OTHER, IS_DISPLAYED, ERROR, GET_PROPERTY,
}